spring:
  application:
    name: API_Libros
  datasource:
    url: jdbc:mysql://localhost:3306/libros
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    open-in-view: false
